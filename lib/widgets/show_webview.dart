import 'package:flutter/material.dart';

class ShowView extends StatelessWidget {
  const ShowView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('This is Showview'),
    );
  }
}