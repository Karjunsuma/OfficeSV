import 'package:flutter/material.dart';

class ReadQRCode extends StatelessWidget {
  const ReadQRCode({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('This is QR Code'),
    );
  }
}